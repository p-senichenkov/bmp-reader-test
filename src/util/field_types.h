#pragma once

#include <cstdint>

namespace bmp {
// Integral types that are used in Microsoft's documentation
using Byte = uint8_t;
using Word = uint16_t;
using DWord = uint32_t;
using Long = int32_t;
}  // namespace bmp
